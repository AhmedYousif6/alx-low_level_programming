#include <stdio.h>

/**
 * main - entry point
 * Describe: print the program name
 * @argc: the number of arguments
 * @argv: array of strings
 * Return: 0 always (success)
*/

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
		return (0);
}
