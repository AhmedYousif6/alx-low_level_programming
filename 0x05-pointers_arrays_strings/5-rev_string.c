#include "main.h"

/**
 * rev_string - function that reverses the string
 * @s: takes a strings
 * Return: void
*/

void rev_string(char *s)
{
	int k = 0;

	while (s[k])
		k++;

	while (k--)
	{
		s[k];
	}

}
